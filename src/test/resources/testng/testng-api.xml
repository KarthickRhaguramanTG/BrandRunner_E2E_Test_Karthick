<!--
    API suite is disabled until real API test classes exist. testUtils.BaseApiTest is now a
    working base (see src/test/java/testUtils/BaseApiTest.java) but has no @Test methods of its
    own, so a suite containing only that class would report "no tests found". Add your API test
    classes below (extending BaseApiTest), then uncomment this suite and wire it into pom.xml's
    surefire <suiteXmlFiles> alongside testng-ui.xml.

    <?xml version="1.0" encoding="UTF-8"?>
    <!DOCTYPE suite SYSTEM "https://testng.org/testng-1.0.dtd">
    <suite name="IFC API End to End Suite">
        <test name="Web based API Test">
            <classes>
                <class name="testUtils.BaseApiTest"/>
                <!-- <class name="ai.metayb.api.web.events.FetchAllEventsTest"/> -->
                <!-- <class name="ai.metayb.api.web.events.FetchEventTest"/> -->
            </classes>
        </test>
    </suite>
-->
